{
  "name": "voice-ledger-oracle",
  "version": "1.0.0",
  "description": "Chainlink CRE workflow — Voice Ledger Proof of Provenance, Event Watcher, and Deforestation Oracle",
  "type": "module",
  "main": "main.ts",
  "scripts": {
    "build": "tsc",
    "postinstall": "bunx cre-setup",
    "simulate:cron": "cd .. && cre workflow simulate workflow --non-interactive --trigger-index 0 --target local-simulation",
    "simulate:log": "cd .. && cre workflow simulate workflow --non-interactive --trigger-index 1 --target local-simulation",
    "simulate:http": "cd .. && cre workflow simulate workflow --non-interactive --trigger-index 2 --http-payload '{\"farm_id\":\"FARM-001\"}' --target local-simulation"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.7",
    "viem": "^2.34.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "^1.2.0"
  }
}
