[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: tests requiring live external services (API keys, network)
    integration: tests requiring database or Redis
    e2e: end-to-end tests requiring Telegram bot or live services
addopts = -v --tb=short --strict-markers
timeout = 30
